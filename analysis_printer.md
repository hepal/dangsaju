# 당사주 프로그램 소스 분석 (printer / Applesoft BASIC)

## 1. 개요

- **파일명**: `printer` (AppleWin 프린터 출력 캡처본)
- **언어**: Applesoft BASIC (Apple II)
- **프로그램 종류**: 당사주(唐四柱) 운세 출력 프로그램
- **지원 연도**: 1894 ~ 1990년 (음양력 변환 테이블 기준)
- **입력**: 성명, 생년월일시, 양음력 구분 (S=양력, 그 외=음력)
- **출력**: PR#1 (프린터)로 당사주 10개 항목 출력

---

## 2. 모듈 구조 (라인 번호별)

| 라인 범위 | 역할 |
|-----------|------|
| 0 ~ 65 | **초기화 및 입력 화면** |
| 70 ~ 105 | **프린터 출력 헤더** (성명, 날짜) |
| 110 ~ 290 | **당사주 12궁 인덱스 계산** |
| 300 ~ 400 | **데이터 파일 10개 읽기** |
| 405 ~ 610 | **당사주 결과 프린터 출력** |
| 620 ~ 720 | **CS 오프셋 서브루틴** (ON YV GOSUB) |
| 3000 ~ 3040 | **사용 횟수 카운터** (CHK 파일) |
| 4000 ~ 4010 | **더미 서브루틴** (미구현) |
| 5000 ~ 5022 | **그림 출력**: LADY BE GOOD.PIC |
| 5050 ~ 5070 | **그림 출력**: A.PIC |
| 10000 ~ 11105 | **데이터 초기화** (날짜 테이블 로드) |
| 11110 ~ 11470 | **양력 → 음력 변환** |
| 11480 ~ 11810 | **음력 → 양력 변환** |
| 11820 ~ 11830 | **날짜 범위 오류 처리** |
| 20000 | **프린터 용지 넘김** |

---

## 3. 입력 처리 (라인 40 ~ 60)

```
입력 변수:
  NA$  : 성명
  YY$  : 년 (2자리, 예: "65" → 1965)
  MM$  : 월 (1~12)
  DD$  : 일 (1~31)
  HH$  : 시 (00~23, 24시간제)
  SM$  : 양음력 구분 ("S"=양력, 그 외=음력)
```

- `NA$ = "W"` → 5000번 (LADY BE GOOD.PIC 출력)
- `NA$ = "M"` → 5050번 (A.PIC 출력)
- 시간 유효성 검사: `HH$` < "00" OR > "23" → 재입력 요구

---

## 4. 날짜 변환 (라인 11110 ~ 11810)

### 4-1. 공통 초기화 (11110 ~ 11150)
- `PY(2) = 28` (윤년 아닌 기본값)
- SM$에 따라 분기

### 4-2. 양력 → 음력 (11160 ~ 11470)
- 입력: "19" + YY$로 4자리 연도 (YR)
- 윤년 판정: 400배수 OR (100배수 아니고 4배수)이면 PY(2)=29
- 해당 연도의 양력 날수(NS) 계산 후 YK(KY) 기준일로 음력 월일 산출
- YY(KY, OM) 테이블로 음력 월 길이 조회
  - 값 ≤ 2: 일반월, LL = 값 + 28
  - 값 = 3xx: 윤달 포함 월 (L1, L2 각각 29 or 30일)
- 출력:
  - `AQ$` = "DIDFUR(양력) YYYY년 MM월 DD일 요일요일"
  - `AW$` = "DMA FUR(음력) YYYY년 ZZ$ MM월 DD일"
  - `ZZ$` = "DBS(윤)" if 윤달

### 4-3. 음력 → 양력 (11480 ~ 11810)
- 역방향: YY(KY,I) 테이블로 음력 누적일수 계산
- YK(KY) 더해 양력 절대 날수로 변환
- PY(MT) 배열로 양력 월일 역산
- NS 계산으로 요일 산출
- 출력 AQ$, AW$ 는 양음력 반전

---

## 5. 당사주 인덱스 계산 (라인 110 ~ 290)

당사주는 **12궁(宮)** 체계. 출생 년·월·일·시 각각을 12로 순환하는 인덱스로 변환.

### 변수 정의
| 변수 | 의미 |
|------|------|
| YV | 년 기반 인덱스 (1~12 정규화) |
| MV | 월 (1~12) |
| DV | 일 (1~31) |
| HV | 시 → `INT((HV+1)/2)` : 24시→12시진 변환 |
| NV | 년도 끝 2자리 mod 10 |
| CS | ON YV GOSUB으로 결정되는 기준 오프셋 |

### 12궁 인덱스 계산 순서
```
라인 110: YV = 1 + VAL(YY$)  (음력 년도 기반, 1-indexed)
          HV = INT((HV + 1) / 2)    ← 24시간 → 12시진
          NV = VAL(YY$)             ← 년도 끝 2자리

라인 120: YV 정규화 (mod 12, 1~12 범위)
          IF YV > 12 THEN YV = YV - 12 : GOTO 120

라인 130: MZ = YV + MV - 1         ← 월궁
          IF MZ > 12 THEN MZ = MZ - 12

라인 140: LZ = MZ + DV - 1         ← 일궁

라인 150: IF LZ > 12 THEN LZ = LZ - 12 : GOTO 150  (반복 정규화)

라인 160: TZ = LZ + HV             ← 시궁
          IF TZ > 12 THEN TZ = TZ - 11   ⚠ 버그 가능성 (아래 참조)

라인 170: ON YV GOSUB 700,620,640,670,...  → CS 설정 (패턴: 9,0,3,6 반복)

라인 180: HZ = CS + MV             ← 월 데이터 인덱스
          IF HZ > 12 THEN HZ = HZ - 12

라인 190: ON YV GOSUB 680,710,630,650,...  → CS 재설정 (패턴: 7,10,1,4 반복)

라인 200: CZ = CS + HV + 1         ← 시 데이터 인덱스
          IF CZ > 12 THEN CZ = CZ - 12

라인 220: NV 정규화 (mod 10, 1~10)
          IF NV > 10 THEN NV = NV - 10 : GOTO 220

라인 230: ON NV GOSUB 660,650,640,620,720,710,700,690,680,670
          → CS 설정 (CS = 5,4,3,0,11,10,9,8,7,6)

라인 240: J1 = CS + MV1            ⚠ 버그: MV1 미정의 (MV여야 할 것)
          IF J1 > 12 THEN J1 = J1 - 12

라인 260: SZ = 14 - YV             ← 역방향 기준
          IF SZ > 12 THEN SZ = SZ - 12

라인 270: SZ = SZ + MV - 1
          IF SZ > 12 THEN SZ = SZ - 12

라인 280: DC(1..10) 배열 확정
  DC(1)=SZ, DC(2)=TZ, DC(3)=YV, DC(4)=MZ,
  DC(5)=LZ, DC(6)=HZ, DC(7)=CZ, DC(8)=J1,
  DC(9)=J1, DC(10)=C2
```

### CS 오프셋 서브루틴 (라인 620~720)
| 서브루틴 라인 | CS 값 |
|--------------|-------|
| 620 | 0 |
| 630 | 1 |
| 640 | 3 |
| 650 | 4 |
| 660 | 5 |
| 670 | 6 |
| 680 | 7 |
| 690 | 8 |
| 700 | 9 |
| 710 | 10 |
| 720 | 11 |

**주목**: CS=2 에 해당하는 서브루틴이 없음 → 특정 년도에서 CS=2가 발생하지 않는 설계인지 확인 필요.

---

## 6. 데이터 파일 시스템 (라인 300 ~ 400)

10개 항목을 각각 별도 파일에서 레코드 번호로 읽음.

| 항목 번호 | 파일명 (FR$) | 레코드 수 (QB%) | 레코드 크기 (B%) | 출력 레이블 |
|-----------|-------------|----------------|-----------------|------------|
| 1 | I | 8 | 500 | CHD      DNS  : (천명 단수?) |
| 2 | D | 3 | 280 | XMRTJDALC DBSUS: |
| 3 | A | 3 | 280 | CH SUS RND  : (초년 운대?) |
| 4 | B | 3 | 280 | WND SUS RND : (중년 운대?) |
| 5 | C | 3 | 280 | AKF SUS RND : (말년 운대?) |
| 6 | E | 3 | 280 | QN QN RND : (관운?) |
| 7 | F | 2 | 280 | WK TLR RND : (직업?) |
| 8 | G | 2 | 280 | WP DLF WLR DJQ : (배우자1?) |
| 9 | H | 2 | 280 | WP DL WLR DJQ : (배우자2?) |
| 10 | J | 2 | 280 | TN GKS RND : (자녀?) |

- 파일 읽기: DOS 3.3 ProDOS 명령 (`CHR$(4);"OPEN/READ/CLOSE"`)
- 각 파일에서 `DC(I)` 번째 레코드 읽기 (1~12 범위)

---

## 7. 출력 흐름 (라인 405 ~ 610)

```
CALL 3327      ← 화면 클리어 (ML 루틴)
PR# 1          ← 프린터 출력 시작
헤더 출력:
  - 성명 (NA$)
  - 양력 날짜 (AQ$)
  - 음력 날짜 + 시 (AW$ + HH$ + "TL TOD")
  - 제목 라인 (KK$ = "   <LDML VUDTOD TKWN ")
당사주 10개 항목 출력 (DB$(I, 0..L%) 순서대로)
GOSUB 4000     ← 더미 (미구현)
POKE 214,18    ← 커서 위치 설정
PRINT CHR$(140) ← 용지 넘김
PR# 0          ← 프린터 끄기
GOTO 40        ← 다음 입력 대기
```

---

## 8. 데이터 초기화 (라인 10000 ~ 11105)

### 로드되는 테이블
| 배열 | 크기 | 내용 |
|------|------|------|
| `PY(12)` | 12 | 월별 일수 (31,28,31,30,31,30,31,31,30,31,30,31) |
| `MO$(7)` | 7 | 요일명 (일~토) |
| `YK(97)` | 97 | 1894~1990년 음력 설날의 양력 날수 오프셋 |
| `YY(97,12)` | 97×12 | 각 연도 음력 월별 날수 코드 |

- `YK`, `MO$`는 DATA 문으로 내장
- `YY`는 외부 파일 **CAL/DAT** 에서 읽음 (`OPEN/READ/CLOSE`)

---

## 9. 기타 기능

### 사용 횟수 카운터 (라인 3000 ~ 3040)
- 파일 **CHK** 에서 카운터 값 읽기 → +1 → 프린터 출력 → 파일에 저장
- 복사 방지 또는 사용량 추적 목적으로 추정

### 특수 명령 처리
- `NA$ = "W"`: LADY BE GOOD.PIC HGR 그림 프린터 출력
- `NA$ = "M"`: A.PIC ($2000) HGR 그림 프린터 출력

---

## 10. 버그 수정 이력

### BUG-1 [수정완료]: MV1 미정의 변수 (라인 240)

| | 내용 |
|---|---|
| 원본 | `240 J1 = CS + MV1: IF J1 > 12 THEN J1 = J1 - 12` |
| 수정 | `240 J1 = CS + MV: IF J1 > 12 THEN J1 = J1 - 12` |

- `MV1`은 프로그램 어디에도 정의되지 않아 Applesoft BASIC이 0으로 취급
- `J1 = CS + 0 = CS` 로 고정되어 DC(8), DC(9) 인덱스가 항상 오계산됨
- `MV` (월 값)로 수정

### BUG-2 [수정완료]: TZ 시궁 정규화 오류 (라인 160)

| | 내용 |
|---|---|
| 원본 | `160 TZ = LZ + HV: IF TZ > 12 THEN TZ = TZ - 11` |
| 수정 | `160 TZ = LZ + HV: IF TZ > 12 THEN TZ = TZ - 12` |

- LZ 최대 12, HV 최대 12 → TZ 최대 24
- `-11` 적용 시 TZ=24 → 13 으로 여전히 12 초과, DC(2) 시궁 값 오류
- `-12` 로 수정하면 최대 TZ=24 → 12 로 한 번에 정규화됨

### BUG-3 [수정완료]: 관운 항목 출력 누락 (라인 520)

| | 내용 |
|---|---|
| 원본 | `520 X = 13: FOR I = 1 TO 3:A$ = DB$(6,I): NEXT I` |
| 수정 | `520 X = 13: FOR I = 1 TO 3:A$ = DB$(6,I): PRINT A$: NEXT I` |

- DB$(6,I) 를 읽기만 하고 출력하지 않아 관운(QN QN RND) 항목 내용이 프린터에 나오지 않음
- 다른 항목(라인 440, 460, 480, 500, 540 등)과 동일하게 `PRINT A$` 추가

### BUG-4 [수정완료]: 윤년 판정 오타 (라인 11670, 11710)

| | 내용 |
|---|---|
| 원본 | `... ( INT (YR / 100) * 10200) <> YR ...` (2곳) |
| 수정 | `... ( INT (YR / 100) * 100) <> YR ...` (2곳) |

- 음력→양력 변환 루틴(11670, 11710)에서 `10200`은 `100`의 오타
- `INT(YR/100) * 10200` 은 YR과 항상 달라 100의 배수 제외 조건이 무력화됨
- 결과: 1900년처럼 윤년이 아닌 100배수 년도도 윤년으로 처리되어 날짜 오차 발생
- 라인 11220 (양력→음력 방향)의 동일 로직은 `100`으로 올바르게 작성되어 있었음

---

## 11. 한글 인코딩 방식

소스 내 한글 텍스트는 Apple II 키보드 배열 기반 로마자 코드로 작성됨.

| 코드 | 추정 한글 |
|------|----------|
| DLF | 일(日) |
| DNJF | 월(月) |
| GHK | 화(火) |
| TN | 수(水) |
| AHR | 목(木) |
| RMA | 금(金) |
| XH | 토(土) |
| DIDFUR | 양력(陽曆) |
| DMA FUR | 음력(陰曆) |
| DBS | 윤(閏) |
| DL FMA | 성명(姓名) |
| LDML VUDTOD TKWN | 당신의 사주팔자? |

- 실제 화면/프린터 출력 시 이 코드들이 Apple II 한글 폰트 ROM을 통해 한글로 표시됨
- 현대 PC에서 읽으려면 별도 변환 테이블 필요

---

## 12. 전체 흐름 요약

```
프로그램 시작
    ↓
10000: 데이터 초기화 (테이블 로드, CAL/DAT 파일 읽기)
    ↓
40: 성명·생년월일시·양음력 입력
    ↓
11110: 날짜 변환 (양→음 or 음→양)
    ↓
110~280: 당사주 12궁 인덱스 계산 (DC 배열 10개 값 결정)
    ↓
300~400: 데이터 파일 10개에서 해당 레코드 읽기 (DB 배열)
    ↓
405~610: 프린터로 결과 출력 (10개 항목)
    ↓
40: 다음 입력으로 복귀
```
